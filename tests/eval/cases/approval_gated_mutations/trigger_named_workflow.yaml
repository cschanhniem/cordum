name: trigger_named_workflow
category: approval_gated_mutations
user_message: "trigger the demo.mock-bank.transfer workflow"
expected_tool_calls:
  - tool_name: cordum_trigger_workflow
    args:
      workflow_id: str
expected_summary_contains:
  - "workflow"
forbidden_tool_calls:
  - cordum_cancel_job
  - cordum_approve_job
max_tool_calls: 1
