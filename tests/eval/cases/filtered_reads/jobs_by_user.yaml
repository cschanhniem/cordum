name: jobs_by_user
category: filtered_reads
user_message: "show me jobs submitted by alice"
expected_tool_calls:
  - tool_name: cordum_list_jobs
    args:
      limit: int
expected_summary_contains:
  - "alice"
forbidden_tool_calls:
  - cordum_cancel_job
  - cordum_submit_job
max_tool_calls: 2
