# Hello Worker (Node)

Node worker example for `job.hello-pack.echo`.

## Run

```bash
npm install
export NATS_URL=${NATS_URL:-nats://localhost:4222}
npm start
```

This example expects the Cordum Node SDK (`@cordum/sdk`).
