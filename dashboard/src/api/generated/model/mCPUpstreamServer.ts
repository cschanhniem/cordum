/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Cordum HTTP API
 * Canonical OpenAPI 3.0.3 spec for the Cordum gateway HTTP surface.
 * OpenAPI spec version: 2026-05-09.2
 */
import type { MCPUpstreamServerTransport } from "./mCPUpstreamServerTransport";
import type { MCPUpstreamServerLabels } from "./mCPUpstreamServerLabels";
import type { MCPUpstreamServerRisk } from "./mCPUpstreamServerRisk";

export interface MCPUpstreamServer {
  /** @pattern ^[A-Za-z0-9.-]+$ */
  name: string;
  transport: MCPUpstreamServerTransport;
  /** HTTPS endpoint for http/sse transports; unsafe local endpoints are rejected in strict policy modes. */
  endpoint?: string;
  /** Shell-free argv vector for stdio transport. Shell metacharacters are rejected. */
  command?: string[];
  /** Tenant scope for the upstream; `*` is reserved for system-wide entries. */
  tenant_id: string;
  /**
   * Reference to auth material. Raw secrets are rejected and never resolved in registry responses.
   * @pattern ^secret://
   */
  auth_secret_ref?: string;
  labels?: MCPUpstreamServerLabels;
  risk: MCPUpstreamServerRisk;
  enabled: boolean;
  /** Registration-time DNS resolution of the upstream hostname.
Server-managed; clients MUST NOT submit values here on write.
Use-time callers (`RevalidateMCPUpstreamAtUse`) re-resolve the
hostname and refuse the dial if any current IP falls into the
SSRF deny-set (loopback, RFC1918, link-local incl.
169.254.169.254, IPv6 ULA, multicast, unspecified). Omitted
for `stdio` transports and for hostnames that fail to resolve
at registration.
 */
  resolved_ips?: string[];
  created_at?: string;
  updated_at?: string;
}
