# NATS server config for local/dev runs.
# No authentication required — use nats.dev-tls.conf or nats.release.conf
# for environments that need TLS and/or auth.
port: 4222
jetstream {
  store_dir: /data/jetstream
  sync_interval: "1s"
}
http_port: 8222
